.results
  This widget is not doing something useful yet.
  It received these params:
  = params.inspect
.perform
  .perform_info
    = image_tag('spinner.gif', :alt => 'Please wait')
    Please wait while we do more stuff
  .error_message
    There was an error
  - form_for :user_data, :url => '/widgets/<%= file_name %>', :builder => TodoFormBuilder do |form|
    = form.text_field :postcode
    = perform_button('Search')
