-form_for([@list, @item]) do |f|
  =f.label :name
  %br
  =f.text_field :name
  %br
  =f.label :description
  %br
  =f.text_area :description
  %br
  =f.select :widgets, ["find_your_nearest_gp", "contact_the_register_office"], {}, {:multiple => true}
  %br
  =f.submit
